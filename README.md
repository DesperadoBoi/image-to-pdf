# ImageToPDF

ImageToPDF - Android-приложение для локального создания одного PDF из нескольких изображений.

## Текущее состояние

Проект находится на раннем этапе разработки. Домашний экран оформлен как dashboard из восьми инструментов, а «Ещё» открывает единый каталог будущих PDF-возможностей. Реально доступны создание PDF из изображений и съёмка первой страницы камерой; остальные инструменты честно помечены «Скоро» и не имитируют работу. Изображения выбираются во внутренней MediaStore-галерее с Recent, реальными альбомами и сохранением порядка; Camera, Android Photo Picker и Files доступны как безопасные системные источники. Android 14 partial access показывается отдельно от полного доступа. В редакторе доступны миниатюры, полноэкранное редактирование выбранной страницы, гибкое изменение порядка, удаление и ручной поворот. Перед генерацией progressive `PdfExportSheet` показывает имя, компактный выбор качества и свёрнутые параметры; системный `CreateDocument` открывается после «Конвертировать», progress/cancel сохранены. После успеха пользователь остаётся в Editor, видит одноразовый banner и компактную карточку последнего PDF; отдельный `PdfResultFragment` с preview, human-readable metadata и Share/Open открывается явно.

## Редактор страниц

- обычное нажатие на миниатюру открывает полноэкранный редактор выбранной страницы;
- горизонтальная лента позволяет переходить между всеми страницами документа;
- основной просмотр поддерживает масштабирование и перемещение изображения;
- поворот влево и вправо доступен через компактные кнопки поверх нижних углов preview;
- прямоугольная обрезка доступна как отдельный режим с reset/cancel/apply;
- ручная коррекция перспективы сохранена в processing pipeline, но скрыта из обычного Image-to-PDF сценария и зарезервирована для будущего Smart Scan;
- изменения не перезаписывают исходное изображение и одинаково применяются к миниатюре, просмотру и PDF.

Подробный контракт редактора описан в [docs/PAGE_EDITOR_SPEC.md](docs/PAGE_EDITOR_SPEC.md).
Структура dashboard, каталога и правила расширения описаны в [docs/UI_FOUNDATION.md](docs/UI_FOUNDATION.md).
Архитектура Gallery/Camera/Files описана в [docs/IMAGE_IMPORT_ARCHITECTURE.md](docs/IMAGE_IMPORT_ARCHITECTURE.md).
Внутренняя галерея и permission model описаны в [docs/IN_APP_GALLERY.md](docs/IN_APP_GALLERY.md).
Параметры и output Uri flow описаны в [docs/PDF_EXPORT_FLOW.md](docs/PDF_EXPORT_FLOW.md).
Экран результата и lifecycle preview описаны в [docs/PDF_RESULT_SCREEN.md](docs/PDF_RESULT_SCREEN.md).

## Главная идея

- офлайн Android-приложение для создания PDF из нескольких изображений;
- без рекламы;
- без регистрации;
- без отправки пользовательских документов на сервер;
- обработка выполняется локально;
- простой пользовательский сценарий.

## Запланированные возможности

На уровне MVP планируются:

- выбор нескольких изображений;
- полноэкранный просмотр страницы с увеличением перед созданием PDF;
- изменение порядка страниц, удаление и поворот;
- создание PDF с выбором качества, размера страницы, ориентации и полей;
- локальное создание PDF с отображением прогресса и возможностью отмены;
- сохранение, открытие и отправка результата через системные механизмы Android.

Подробности зафиксированы в [PRODUCT.md](PRODUCT.md).

## Технологический стек

- Android, один модуль `app`;
- production-код на Java;
- UI на XML Views;
- Gradle `9.4.1`;
- Android Gradle Plugin `9.2.1`;
- `compileSdk`: `release(36) { minorApiLevel = 1 }`;
- `targetSdk`: `36`;
- `minSdk`: `24`;
- Java compatibility: `11`;
- AndroidX AppCompat `1.7.1`;
- Material Components `1.14.0`;
- ConstraintLayout `2.2.1`;
- Activity KTX `1.13.0`;
- JUnit `4.13.2`;
- AndroidX Test Ext JUnit `1.3.0`;
- Espresso `3.7.0`.

## Требования для локального запуска

- Android Studio;
- Android SDK;
- встроенный в Android Studio JDK.

## Команды для Windows PowerShell

```powershell
.\gradlew.bat assembleDebug
.\gradlew.bat test
.\gradlew.bat lint
```

## Документация

- [PRODUCT.md](PRODUCT.md) - продуктовые рамки и MVP.
- [ARCHITECTURE.md](ARCHITECTURE.md) - целевая архитектура и технические ограничения.
- [CONTRIBUTING.md](CONTRIBUTING.md) - правила совместной работы.

## Оптимизация PDF

- Создаваемые PDF автоматически оптимизируются по размеру за счёт ограничения растрового разрешения фактической областью изображения на каждой странице.
- Документы обрабатываются локально, без отправки на сервер и без заметного избыточного разрешения bitmap в итоговом PDF.
